# 2026-08-23

## 搭建 Hugo 个人网站骨架(D:\blog)

用户选择 Hugo 方案,已从零搭建完成:

- **Hugo**: v0.165.0 extended,winget 装失败(符号链接权限问题),改为手动下载 GitHub Release 至 `C:\Users\86158\.workbuddy\binaries\hugo\hugo.exe`,已追加进用户 PATH。
- **主题**: PaperMod(git submodule,`themes/PaperMod`)。
- **配置**: `hugo.toml` 中文站(首页/文章/关于/标签导航,homeInfoParams 简介,自动暗色模式,TOC/阅读时间/字数/代码复制)。
- **内容**: `content/_index.md`(首页)、`content/about.md`(关于)、`content/posts/2026-08-23-hello-world.md`(示例文章)、`archetypes/default.md`(完整 Front Matter 模板)。
- **部署**: `.github/workflows/deploy.yml`(checkout submodules → peaceiris/actions-hugo → hugo --minify → upload-pages-artifact → deploy-pages)。
- **构建验证**: `hugo --minify` 成功,12 页面(含 sitemap/RSS/404)。
- **git**: 已 init(main 分支)+ 首次提交 `788da9b`,git 身份为占位符 YourName/you@example.com,待用户改。

**关键坑点(已踩)**:
1. winget 安装 Hugo 会因 create_symlink 权限失败 → 手动下载 zip 到托管目录。
2. curl.exe(Windows 原生)不识别 Git Bash 的 `/tmp` 路径 → 需用 Windows 风格路径。
3. hugo.toml 中 `[menu.main]` 与 `[[menu.main]]` 混用报 "key main already exists as a table" → 用 `[menu]` + `[[menu.main]]` 包裹。

**待办(用户操作)**:
1. 创建 GitHub 仓库 `username.github.io` 并 push。
2. GitHub 仓库 Settings → Pages → Source 选 GitHub Actions。
3. 改 `hugo.toml` 的 baseURL 为实际域名;改 git 身份。
4. (可选)自定义域名 + DNS CNAME/A 记录。

**更新**:用户 GitHub 用户名 = `15829959675`,baseURL 已改为 `https://15829959675.github.io/`(commit 622d841),构建验证通过。待办:建仓库 `15829959675.github.io` → push → 开 Pages。

## 部署上线(23:59-00:15)

- 用户已创建仓库 `15829959675.github.io` 并提供 PAT。
- **坑点**:PAT 推 `.github/workflows/*.yml` 必须勾选 `workflow` scope(否则 remote rejected);已让用户编辑令牌勾选。
- 非交互环境无 tty:GCM approve 与 cmdkey 写入均失败,最终用 `credential.helper store` 持久化(`~/.git-credentials` 明文,已提醒用户可撤销令牌)。
- **坑点**:仓库默认 Pages 为 legacy 模式(服务 main 分支源码)→ 网站 404;通过 `PUT /repos/.../pages {"build_type":"workflow"}` 切换为 GitHub Actions 模式(HTTP 204),再 workflow_dispatch 触发重新部署。
- 上线成功:https://15829959675.github.io/ HTTP 200,内容渲染正常。
- git 身份:`15829959675` / `15829959675@users.noreply.github.com`(仓库级)。
